{
  "name": "scripts",
  "version": "1.0.0",
  "description": "",
  "main": "upload_script.js",
  "scripts": {
    "upload": "./generate_zip.sh && node upload_script.js",
    "upload-dev": "./generate_zip.sh && API_VERSION=dev node upload_script.js",
    "upload-nikki-dev": "./generate_zip.sh && API_VERSION=-42 node upload_script.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.88.2"
  }
}
